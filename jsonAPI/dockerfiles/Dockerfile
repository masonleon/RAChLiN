FROM tomcat:9.0

# TODO change my .war file
# TODO for now update the $CATALINA_HOME directory manually from within the container
#ADD basic-struts2.war /usr/local/tomcat/webapps/

EXPOSE 8080

CMD ["catalina.sh", "run"]
